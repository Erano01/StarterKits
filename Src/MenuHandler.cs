
public interface MenuHandler 
{
    // need UI Menu click listener
    void onClick(); // (InventoryClickEvent event)

    void onOpen(); // (InventoryOpenEvent event)

    void onClose(); //(InventoryCloseEvent event)
}